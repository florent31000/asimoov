// Copy this file to secrets.h and fill in your network, then set
// ASIMOOV_WIFI_ENABLED to 1 in config.h.
//
// secrets.h is git-ignored and must never be committed: it holds a real
// Wi-Fi password.

#pragma once

#define WIFI_SSID "your-network"
#define WIFI_PASS "your-password"
