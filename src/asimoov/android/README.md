# android (WS7)

Android glue: runtime permissions, foreground service. Paired with the
top-level `android/` packaging directory (buildozer, Dockerfile, patches).

Owned by WS7. See `plan.md` section 4.10.
