# tools

Standalone scripts used during development, not part of the installed
package: `record_percepts.py` (WS1), `download_models.py` (WS3),
`latency_report.py` (WS1).

See `plan.md` section 4.11.
